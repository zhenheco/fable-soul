{
  "name": "fable-soul",
  "version": "1.1.0",
  "private": true,
  "description": "Fable judgment layer skill package",
  "scripts": {
    "check": "python3 scripts/validate_skill.py && npm run test",
    "lint": "python3 scripts/validate_skill.py",
    "test": "python3 -m unittest discover -s tests -v"
  }
}
